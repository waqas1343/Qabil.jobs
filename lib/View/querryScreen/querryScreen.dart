import 'package:flutter/cupertino.dart';

class Querryscreen extends StatelessWidget {
  const Querryscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Querry Screen'),
    );
  }
}
